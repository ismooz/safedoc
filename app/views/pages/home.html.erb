
<div class="container bg-white">

  <% if user_signed_in? %>

  <div>

    <h2><i class="far fa-clock"></i> Mes prochaines deadlines</h2>
    <br>

    <div class="row">
      <% if @documents_sorted_deadline.length > 0%>

        <% @documents_sorted_deadline.each do |document| %>
            <div class="col-xs-12 col-md-6 col-lg-4">
              <div class="card-product">
                <%= link_to document do %>
                  <div>
                    <%= cl_image_tag(document.photos.first.key) %>
                  </div>
                  <div class="card-product-infos">
                    <div>
                      <h2><%= document.name %></h2>
                      <% if document.expire.presence && document.expire > 0 && document.expire < 30 %>
                        <p style="color: red; font-weight: bold;"><%= "#{document.expire_string}" %></p>
                      <% else document.expire.presence && document.expire > 0 %>
                        <p><%= "#{document.expire_string}" %></p>
                      <% end %>
                    </div>
                    <div class="btn-group">
                      <%= link_to 'View', document_path(document), class: "btn btn-sm btn-outline-secondary rounded-left" %>
                      <%= link_to 'Edit', edit_document_path(document), class: "btn btn-sm btn-outline-secondary rounded-right" %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
        <% end %>
      <% else %>
        <div class="col-xs-12 col-md-6 col-lg-4">
          <div>
            <em>Bon travail ! Pas d'échéance en cours...</em>
          </div>
        </div>
      <% end %>

    </div>
  </div>

  <hr>

  <div class="pt-5 mb-5">

      <h2><i class="far fa-clock"></i> Mes derniers documents sauvegardés</h2>
      <br>

    <div class="row">
      <% if @documents_sorted_creation.length > 0 %>
        <% @documents_sorted_creation.each do |document| %>

          <div class="col-xs-12 col-md-6 col-lg-4">

            <div class="card-product">
              <%= link_to document do %>

                <div>
                  <%= cl_image_tag(document.photos.first.key) %>

                  <div class="card-product-infos">

                    <div>
                      <h2><%= document.name %></h2>
                      <p><%= "Created at #{ document.created_at.strftime("%d.%m.%Y") }" %></p>
                    </div>
                      <div class="btn-group">
                        <%= link_to 'View', document_path(document), class: "btn btn-sm btn-outline-secondary rounded-left" %>
                        <%= link_to 'Edit', edit_document_path(document), class: "btn btn-sm btn-outline-secondary rounded-right" %>
                      </div>
                  </div>

                </div>

              <% end %>
            </div>
          </div>
        <% end %>
      <% else %>

        <div class="col-xs-12 col-md-6 col-lg-4">

          <div>
            <em>Pas de documents ajoutés pour l'instant...</em>
            <%= link_to '<i class="fas fa-plus"></i> Add a new doc'.html_safe, new_document_path, class: "btn btn-primary"  %>
          </div>

        </div>
      <% end %>
    </div>

  </div>

  <% else %>

  <div class="mb-5">

    <div class="title1 mb-5 text-center">
      <h1>Classez vos documents 📄 intelligemment, <br> Utilisez Safedoc !</h1>
      <div>
        <em>"Safedoc est une application créée pour vous aider à sauvegarder tous vos documents importants au même endroit et gère l'échéance de vos contrats."</em>
      </div>

    </div>

        <hr>

      <div class="d-flex mt-5 mb-5">
        <%= image_tag "iphone.png", style: "height:400px;width:auto;", class: "img1-homepage"%>

        <div class="ml-5 my-auto text1-hompage">
          <h2>🖨 Scanez avec votre smartphone</h2>
          <p>Ne perdez plus constamment vos garanties, contrats et autres documents. Retrouvez-les tous dans une seule application. Sécurisée, pratique et indispensable. L'application "made in 🇨🇭 Switzerland 🇨🇭".</p>
          <%= link_to "S'enregistrer !", new_user_registration_path, class: "btn btn-outline-info" %>
        </div>

      </div>

      <hr>

      <div class="d-flex mt-5">

        <div class="mr-5 my-auto text1-hompage text-right">
          <h2>🔎 Trouvez vos documents !</h2>
          <p>Vous pouvez facilement retrouver vos documents où que vous soyez. Modifiez-les ou ajoutez simplement un rappel pour ne pas oublier la date d'échéance...</p>
          <%= link_to "S'enregistrer !", new_user_registration_path, class: "btn btn-outline-info" %>
        </div>
        <%= image_tag "doc_preview.png", style: "height:300px;width:auto;", class: "img2-homepage"%>

      </div>

    </div>
  <% end %>
</div>




